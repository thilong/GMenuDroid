ext {
    compileSdk = 34
    minSdk = 24
    targetSdk = 24
    versionCode = 1
    versionName = "1.0"

    Java = JavaVersion.VERSION_1_8
}


//disable all tests in all projects
allprojects {
    tasks.withType(Test).tap {
        configureEach {
            enabled = false
        }
    }
}